---
description: Cursor 团队版 VitePress 文档站维护规则
globs:
  - "docs/**"
  - "docs/.vitepress/**"
alwaysApply: true
---

# Cursor 团队版文档站维护规则

## 项目概述

这是一个 VitePress 文档站，域名 docs.liandan.cc，用于 Cursor 团队版（按量计费 / 按次计费）的产品介绍、使用指南、常见问题和公告。

## 目录结构

```
docs/
├── .vitepress/config.mts    # 站点配置（导航、侧边栏）
├── public/images/            # 截图等静态资源
├── index.md                  # 首页（Hero 布局）
├── plans/                    # 套餐介绍
│   ├── team-metered.md       # 按量计费版
│   └── team-request.md       # 按次计费版
├── faq.md                    # 通用常见问题
├── guide/                    # 使用指南（各类问题解决）
├── notice/                   # 公告（时效性内容）
└── contact.md                # 交流群
```

## 侧边栏分组逻辑

侧边栏通过 `config.mts` 中的 `sidebarMain()` 函数统一管理，所有 `/plans/`、`/faq`、`/guide/` 路径共用同一个侧边栏。分组规则：

- **团队版 - 按量计费**：按量计费相关的介绍和指南
- **团队版 - 按次计费(已绝版)**：按次计费相关的介绍和指南，以及按次计费专属问题（如 Max Mode、进度条显示异常等）
- **通用指南**：两个版本都适用的指南（如切换账号、网络问题）
- **常见问题**：通用 FAQ，每条指向 faq.md 的锚点

新增内容时必须判断它属于哪个分组。仅适用于某个版本的内容放到对应版本下，通用内容放到通用指南。

## 新增页面的标准流程

1. **判断归属**：内容属于按量计费、按次计费、还是通用？是否适合放进已有页面还是单开新页面？
2. **创建 md 文件**：放到 `docs/guide/`（指南类）、`docs/notice/`（公告类）或相应目录
3. **更新 config.mts**：在 `sidebarMain()` 的对应分组中添加条目，同时更新 `nav` 的使用指南下拉菜单
4. **处理图片**：用户上传的图片复制到 `docs/public/images/`，md 中用 `![描述](/images/文件名.png)` 引用
5. **构建验证**：`yarn build` 确认无报错
6. **提交推送**：`git add -A && git commit && git push`（如果 SSH push 失败提醒用户手动 push）

## 写作风格

- 正式但不生硬的文档语气，不用"亲~"等电商话术
- 用户提供的口语化内容必须润色为文档风格，但保留核心信息
- 关键数字、规则、警告用 **加粗** 或 VitePress 容器突出：
  - `:::tip` 提示/技巧
  - `:::warning` 注意事项
  - `:::danger` 严重警告（如 Max Mode 禁令）
  - `:::info` 补充说明
- 每页开头用一两句话概括核心信息
- 页面之间通过链接互相引用，形成网状结构

## 图片处理

- 用户上传的图片保存在 Cursor 的 assets 目录，需要手动复制到 `docs/public/images/` 并取一个语义化文件名
- md 中引用路径从 `/images/` 开始，不写 `public`
- 图片必须有 alt 描述文字

## 公告页面

公告放在 `docs/notice/` 目录，文件名格式 `YYYYMMDD-简短描述.md`。公告侧边栏独立配置，不在 sidebarMain() 中。如有新公告需要同时更新首页的 tip 容器和公告侧边栏。

## FAQ 页面

faq.md 是单页多问题结构，用二级标题分隔。侧边栏中每个问题指向对应锚点。新增 FAQ 时：
1. 在 faq.md 中添加内容
2. 在 sidebarMain() 的常见问题分组中添加锚点链接

注意：仅适用于某个版本的问题不放在 FAQ 里，而是单开页面放到对应版本分组下。
